{extends ../../../templates/@portal.latte}
{varType App\Modules\CashBoxes\Domain\Entity\AnnualFinancialReport[] $reports}
{varType bool $isPrivileged}
{varType ?string $message}
{varType ?string $error}

{block main}
  {include
    ../../../templates/components/header-page.latte,
    title => 'Informes financieros anuales',
    description => 'Consulta los informes cargados por año y descarga el archivo oficial.',
    icon => 'bi-file-earmark-text'
  }

  <div class="d-flex flex-wrap justify-content-between align-items-center gap-3 mb-4">
    <p class="text-muted mb-0">Lista general de informes financieros anuales.</p>
    {if $isPrivileged}
      <a href="./gestionar.php" class="btn btn-outline-primary rounded-pill px-4">
        <i class="bi bi-gear me-2"></i>Gestionar
      </a>
    {/if}
  </div>

  {if $message}
    <div class="alert alert-success border-0 rounded-4 mb-4" role="alert">
      <i class="bi bi-check-circle me-2"></i>{$message}
    </div>
  {/if}

  {if $error}
    <div class="alert alert-danger border-0 rounded-4 mb-4" role="alert">
      <i class="bi bi-exclamation-triangle me-2"></i>{$error}
    </div>
  {/if}

  <div class="card border-0 shadow-sm rounded-4">
    <div class="table-responsive">
      <table class="table table-hover align-middle mb-0">
        <thead class="bg-light text-uppercase small text-muted">
          <tr>
            <th class="px-4 py-3 border-0">Año</th>
            <th class="px-4 py-3 border-0">Documento</th>
            <th class="px-4 py-3 border-0 text-end">Acciones</th>
          </tr>
        </thead>
        <tbody>
          {foreach $reports as $report}
            <tr class="transition-all">
              <td class="px-4 py-3 fw-bold text-dark">{$report->year}</td>
              <td class="px-4 py-3">
                <div class="d-flex flex-column">
                  <span class="fw-medium text-dark">{$report->fileName()}</span>
                  <small class="text-muted">Archivo cargado para el ejercicio {$report->year}</small>
                </div>
              </td>
              <td class="px-4 py-3 text-end">
                <div class="d-inline-flex flex-wrap justify-content-end gap-2">
                  <a href="{download($report->document)}" target="_blank" rel="noopener" class="btn btn-outline-primary btn-sm rounded-pill px-3">
                    <i class="bi bi-download me-1"></i>Descargar
                  </a>
                  <a href="./ver.php?id={$report->id}" class="btn btn-outline-secondary btn-sm rounded-pill px-3">
                    <i class="bi bi-eye me-1"></i>Ver
                  </a>
                </div>
              </td>
            </tr>
          {else}
            <tr>
              <td colspan="3" class="text-center py-5 text-muted">
                <i class="bi bi-inbox fs-1 d-block mb-3 opacity-25"></i>
                No hay informes cargados todavía.
              </td>
            </tr>
          {/foreach}
        </tbody>
      </table>
    </div>
  </div>
{/block}